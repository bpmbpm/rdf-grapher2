{
  "name": "rdf-grapher2-ver1",
  "private": true,
  "scripts": {
    "build": "tsc && node scripts/build-html.mjs",
    "check": "tsc --noEmit",
    "test": "npm run build && node --test tests/*.test.js",
    "test:browser": "node tests/browser-smoke.mjs"
  },
  "devDependencies": {
    "playwright": "1.62.1",
    "typescript": "5.9.2"
  }
}
